const app = Vue.createApp({
    // data, functions
    // template: '<h1>I am a templete,</h1>'         this would be very tricky to give multiple elements here
    // thats whay we use templete element direct in html file
})

app.mount('#app')